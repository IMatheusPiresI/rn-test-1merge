import Realm from 'realm';
import { UserSchema } from './schemas/userSchema';

// export const getRealm = async () => {
//   await Realm.open({
//     path: 'test-mage',
//     schema: [UserSchema],
//   });
// };
