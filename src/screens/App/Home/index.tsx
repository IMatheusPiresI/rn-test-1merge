import { Text, VStack } from 'native-base';
import React from 'react';
import 'react-native-gesture-handler';

export const Home: React.FC = () => (
  <VStack bgColor="#FAFAFA" flex={1}>
    <Text>Open up App.tsx to start working on your !</Text>
  </VStack>
);

export default Home;
